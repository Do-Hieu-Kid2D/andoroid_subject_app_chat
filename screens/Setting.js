import React, {useState} from 'react';
import {
    Image,
    Text,
    View,
    ImageBackground,
    StyleSheet,
    TouchableOpacity,
    Alert,
    Modal,
    Button,
} from 'react-native';

import {images, icons, texts, colors, numbers} from '../constants/index';

function Chat() {
    return (
        <View>
            <Text
                style={{
                    color: 'black',
                }}>
                Setting
            </Text>
        </View>
    );
}

export default Chat;
